{
  "name": "bonusprogramm-webapp",
  "version": "1.0.0",
  "dependencies": {
    "express": "^4.18.2",
    "body-parser": "^1.20.2"
  },
  "scripts": {
    "build": "mkdir -p public && cp index.html public/index.html && cp admin.html public/admin.html && cp dashboard.html public/dashboard.html && cp style.css public/style.css"
  }
}